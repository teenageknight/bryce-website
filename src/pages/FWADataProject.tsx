import React from "react";

export function FWADataProjectPage() {
    return (
        <>
            <p>Upload map spreadsheets</p>
            {/* Preformat for now and write that code later */}
            <p>Button to upload unformatted excel</p>
            {/* On button press, format the spreadsheet, and geocode results to get api repsonse */}
            {/* Generate a formatted map */}
            {/* Output the embedded iframe code */}
        </>
    );
}
