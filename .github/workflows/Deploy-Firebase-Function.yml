on:
    workflow_call:
        inputs:
            config-path:
                required: true
                type: string
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - run: echo 'hello world'
